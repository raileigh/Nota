{% extends "::base.html.twig"%}
{% block body %}
  <form class="form-horizontal" action="{{ url('notes_crear') }}" method="post">
      <div class="form-group, text-center, col-xs-12" style="padding-top:30px">
        <label class="control-label, col-xs-1">TITULO</label>
        <div class="col-xs-4">
          <input type="text" class="form-control" name="titulo">
        </div>
        <label class="control-label, col-xs-1">CONTENIDO</label>
        <div class="col-xs-4">
          <input type="text" class="form-control" name="contenido">
        </div>
        <div class="form-group, col-xs-2">
        <div class="col-xs-2">
          <button type="submit" class="btn btn-default" name="crear">CREAR</button>
        </div>
        </div>
      </div>
    </form>
    <br>
    <br>
    <br>
    <br>
    <br>
    {% for nota in notas %}
        <div class="panel panel-default">
          <div class="panel-heading">
            {{nota.fecha|date("d/m/Y H:m:i")}}<br>
            <div class="hidden">
            id:{{nota.id}}
            </div><br>
            <h3 class="panel-title">Título:{{nota.titulo}}</h3>
          </div>
          <div class="panel-body">Contenido:{{nota.contenido}}
          </div>
        </div>
        <a href="{{ path('notes_eliminar', { id: nota.id }) }}" class="btn btn-default btn-md"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></a>
        
        <a data-toggle="modal" href="{{ path('notes_actualizar', { id: nota.id }) }}" data-target="#myModal1" class="btn btn-default btn-md"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
        <div id="myModal1"></div>
        <!-- register form here -->
        <br>
  {% endfor %}
 
{% endblock %}  
  







